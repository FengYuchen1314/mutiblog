# MutiBlog 项目说明书

> 文档版本：0.1
> 状态：产品方向确认稿
> 日期：2026-08-10
> 面向对象：项目所有者、产品设计者、开发者、AI 编程代理

---

## 0. 文档用途与权威性

本文档记录 MutiBlog 重构后的真实产品意图，用于防止设计和实现再次偏离。

### 0.1 权威规则

1. 本文档是新 `main` 分支的最高产品依据。
2. 项目所有者在后续对话中的明确决定高于本文档；决定发生变化后，必须同步更新本文档。
3. Halo 和 HedgeDoc 只提供产品与交互基线，不能覆盖 MutiBlog 已确认的独有规则。
4. 实现方便、现有代码、历史文档或技术偏好均不能覆盖本文档。
5. `old` 分支仅为历史归档，禁止读取、引用、复制或改造其中的代码、文档、接口与架构。

### 0.2 状态标记

- **已确认**：项目所有者已明确确认，实施时不得自行改变。
- **实施自主权**：目标已确认，具体技术方案由开发者负责。
- **待确认**：尚未形成产品决定，不得假定为最终需求。

---

## 1. 产品定义

MutiBlog 是一个面向公众发布的、开源的、单站点、单管理员、自托管、多语言 AI 原生内容管理与发布系统。

产品由两部分组成：

1. **Halo 式 CMS 产品外壳**：后台的信息架构、操作框架、页面布局、功能逻辑和交互体验以 Halo 2.25 社区版核心 CMS 为基线。
2. **MutiBlog 独有发布引擎**：以 Markdown/YAML 为真相源，使用 AI 管理多语言内容，并将公开页面构建为静态文件；评论是明确的动态例外。

简化表达：

> Halo 的使用体验 + HedgeDoc 风格 Markdown 编辑器 + 文件真相源 + AI 多语言 + 静态发布。

---

## 2. 不可妥协的核心原则

以下原则均已确认。

### 2.1 文件是真相源

- Markdown 和 YAML 文件是永久数据的唯一真相源。
- 文章、页面、分类、标签、菜单、友链、站点设置、多语言数据和评论均不得只存在数据库中。
- SQLite 只能保存可丢弃、可重建的运行状态或索引。
- 删除 SQLite、缓存和静态生成目录后，必须能够从永久文件完整重建系统。

### 2.2 公开内容静态化

- 文章、页面、首页、归档、分类、标签、友链、搜索索引和 SEO 文件在发布或构建时生成。
- 已发布内容由静态服务器直接提供。
- CMS 宕机时，已发布正文必须继续正常访问。
- 访客读取正文时不得临时解析 Markdown、调用 AI 或依赖内容数据库。

### 2.3 评论是动态例外

- 评论通过动态 API 加载和提交。
- CMS 宕机时，正文照常可读，评论区域显示“评论服务暂时不可用”。
- 评论永久数据保存在本地 YAML 文件中。
- SQLite 可以保存评论索引，但索引必须可从 YAML 重建。

### 2.4 AI 多语言是核心能力

- AI 多语言不是插件或附加按钮，而是核心内容工作流。
- AI 不得成为安装、源语言写作和源语言发布的硬依赖。
- 没有 API Key 时，系统仍能正常完成初始化、写作、手工翻译和源语言发布。

### 2.5 单体、低资源、自托管

- 主要交付形式保持一个容器和一套安装流程。
- 不引入必须单独维护的 Redis、PostgreSQL、Elasticsearch、消息队列或 Kubernetes。
- 允许混用 Go、TypeScript/Node 或其他合适语言。
- 不再要求“所有 API 必须由 Go 实现”或“Node 只能负责渲染”。
- 语言选择以开发舒适度、可靠性和可维护性为准，但不能导致无意义的进程与运行时膨胀。

---

## 3. 产品与交互基线

### 3.1 Halo 基线

后台以 Halo 2.25 社区版核心 CMS 为直接产品参照，包括：

- 整体控制台外壳；
- 导航结构；
- 仪表盘；
- 列表、筛选、分页和批量操作；
- 表单、抽屉、弹窗、操作菜单和状态反馈；
- 文章、页面、分类、标签、附件、菜单和评论管理；
- 回收站；
- 备份与恢复；
- 站点设置；
- 主题的完整管理生命周期。

允许在布局、交互和视觉上做到与 Halo 高度一致甚至接近一模一样，但必须：

- 使用 MutiBlog 名称与品牌；
- 独立实现，不直接复制 Halo 的后端技术架构；
- 不使用 Halo 的名称、Logo 或不属于开源许可范围的品牌资产；
- 将所有数据操作接入 MutiBlog 的文件真相源、多语言和静态发布模型。

Halo 官方参考：

- <https://github.com/halo-dev/halo>
- <https://docs.halo.run/2.23/category/%E7%94%A8%E6%88%B7%E6%8C%87%E5%8D%97>

### 3.2 明确不复制的 Halo 能力

- Halo 专业版功能；
- Halo 商城版；
- 商品、订单、支付；
- 手机 App 和商业云服务；
- 多用户与 RBAC；
- 通用插件运行时；
- 应用市场；
- Halo 的 Java、Spring、数据库和 Thymeleaf 后端架构。

---

## 4. 站点与管理员模型

### 4.1 单站点

- 一个 MutiBlog 实例只管理一个站点。
- 不支持多租户。
- 不支持一个后台管理多个博客或多个独立站点。
- 名称中的 “Muti” 表达产品品牌和多语言方向，不代表多站点。

### 4.2 单管理员

- 首次初始化创建唯一管理员。
- 不提供用户列表、邀请、角色、权限矩阵或团队协作。
- 不提供作者、编辑、翻译者、审核员等角色。
- 保留登录、退出、密码修改、密码重置、会话安全和安全审计。
- 所有文章作者信息默认来自站点管理员或站点资料，而不是多用户系统。

### 4.3 后台语言

- 首次初始化时选择后台界面语言。
- 整个后台只有一个当前界面语言，不按用户分别保存。
- 管理员之后可以在设置中切换后台语言。
- 切换后整个后台统一使用新语言，并持久化到配置中。

---

## 5. 内容模型

### 5.1 Halo 核心内容能力

常规 CMS 内容能力以 Halo 2.25 社区版为基线，至少包括：

- 文章；
- 独立页面；
- 层级分类；
- 标签；
- 附件与媒体库；
- 菜单与菜单项；
- 友链及友链分组；
- 评论；
- 站点设置；
- SEO 设置；
- 主题与主题设置；
- 备份、恢复、导入和导出。

这些常规能力不需要重新发明产品逻辑，原则上复刻 Halo 的操作框架，再接入 MutiBlog 的数据模型。

### 5.2 所有公开数据都必须多语言

多语言不只用于文章正文。以下所有访客可见数据都必须支持语言版本：

- 站点标题、副标题和描述；
- 文章标题、摘要、正文与 SEO；
- 独立页面标题、正文与 SEO；
- 分类名称、描述与 SEO；
- 标签名称与 SEO；
- 菜单名称和菜单项文字；
- 友链名称、分组和描述；
- 默认主题界面文案；
- 评论区界面文案；
- 搜索、空状态、错误页和系统公开提示。

---

## 6. URL 与永久标识

### 6.1 语言前缀

- 所有语言均使用 URL 前缀，源语言也不例外。
- 示例：`/zh-cn/...`、`/en/...`、`/ja/...`。
- `/` 不承载具体语言内容，只进行语言协商并返回 302 跳转。

### 6.2 永久路径标识

文章、页面、分类、标签及其他公开实体使用统一规则：

- 默认生成紧凑 UUID，例如 `550e8400e29b41d4a716446655440000`；
- 创建时允许管理员手工指定；
- 手工值只允许小写英文字母和连字符；
- 推荐校验表达式：`^[a-z]+(?:-[a-z]+)*$`；
- 不允许数字、中文、下划线、首尾连字符或连续连字符；
- 创建后永久固定，不允许修改；
- 同一实体的所有语言版本共用同一永久标识。

### 6.3 可选生成策略

- 系统支持将新实体的默认标识生成策略改为毫秒时间戳。
- 默认策略仍为紧凑 UUID。
- 配置变更只影响之后新建的实体，不修改已有 URL。

---

## 7. 多语言规则

### 7.1 源语言

- 每个站点只有一个固定源语言。
- 创建文章时不能逐篇选择源语言；新资源继承当时的站点源语言。
- 源语言在站点级别配置。
- 其他语言均为派生语言。
- 管理员可以切换站点源语言；切换只决定之后新建资源的原始源语言和站点默认入口，既有文章、页面及其他永久资源继续保留创建时的原始源语言。
- 仍被既有永久资源作为原始源语言使用的语言不能直接禁用或移除，以保证旧内容可编辑、可构建且既有 URL 不消失。

### 7.2 目标语言

- 提供常用语言预置。
- 允许添加任意合法 BCP 47 语言代码。
- 编辑器语言标签、翻译矩阵、URL、任务和发布内容必须全部由配置动态生成。
- 禁止在代码中写死 `zh-CN`、`en`、`ja`、`de` 等语言集合。

### 7.3 内容回退与跳转

请求某语言内容但该语言版本缺失时，使用以下顺序：

```text
请求语言 → en → zh-CN → sourceLocale → 404
```

其中实体详情的 `sourceLocale` 是该实体创建时保留的原始源语言；站点聚合页面使用当前站点源语言。

回退时必须跳转到实际语言 URL：

- 例如访问 `/ja/posts/<id>/`，日文不存在但英文存在，则 302 到 `/en/posts/<id>/`；
- 禁止在 `/ja/` URL 下直接显示英文正文；
- 回退关系在构建阶段生成，不依赖访客请求时查询 CMS 内容。

### 7.4 框架自身多语言

- 管理后台、默认主题、系统提示和公开组件必须自带多语言能力。
- 新增语言时，系统必须检查框架文案的完整性。
- 自定义站点内容主要通过 AI 或人工翻译。
- 框架字典、站点数据和文章译文都属于可管理、可校验的正常数据，不允许散落在代码常量中。

---

## 8. Markdown 编辑器

文章和页面编辑器不复刻 Halo 的富文本编辑器，而采用 HedgeDoc 风格的纯 Markdown 体验。

### 8.1 基本模式

- 仅 Markdown 编辑；
- 仅预览；
- Markdown 与预览左右分栏；
- 支持全屏写作；
- 支持编辑区与预览区同步滚动；
- 预览必须使用与正式发布相同的 Markdown 渲染管线。

HedgeDoc 参考：<https://docs.hedgedoc.org/references/url-scheme/>

### 8.2 编辑能力

- 纯 Markdown 源码，不做所见即所得；
- 支持常用 Markdown 快捷键和工具栏；
- 支持粘贴图片；
- 支持拖入图片；
- 图片自动上传到本地媒体库；
- 上传成功后在当前光标位置插入标准 Markdown 图片语法；
- 不实现 HedgeDoc 的实时多人协作和权限系统。

### 8.3 译文编辑

- 默认显示“目标语言 Markdown + 目标语言预览”；
- 可以展开只读源文对照栏；
- 展开后形成“源文只读 / 译文编辑 / 译文预览”三栏；
- 支持源文与译文按段落定位；
- 不能在派生语言编辑状态中修改源文。

---

## 9. AI 翻译产品规则

### 9.1 触发规则

- 源文章首次发布时，自动翻译所有已启用目标语言。
- 源文之后修改并重新发布时，只将相关译文标记为过期。
- 源文更新不得自动重新翻译并覆盖已有译文。
- 管理员可以逐语言或批量选择重新翻译。
- 文章正文默认分段翻译，不提供“整篇一次性发送”作为默认路径；页面正文使用同一分段引擎。

### 9.2 人工译文保护

- 人工维护的译文永远不得被后台任务自动覆盖。
- 如果人工译文被管理员明确加入单次或批量翻译范围，必须二次确认。
- 二次确认必须允许管理员排除人工译文或明确覆盖。

### 9.3 没有 AI 配置时

- 可以正常初始化和登录；
- 可以创建、编辑和发布源语言内容；
- 可以手工创建其他语言版本；
- AI 操作入口提示管理员先配置 Provider；
- AI 不可用不得阻止源语言发布。

### 9.4 技术实现自主权

以下内容由开发者负责，不需要项目所有者逐项设计：

- Markdown 分段；
- 代码、链接、数学公式和占位符保护；
- 超长文章切分；
- Token 估算和预算计划；
- 重试、退避、熔断和恢复；
- 卡住任务检测；
- 幂等、断点续传和任务去重；
- AI 输出结构校验；
- Provider 兼容层；
- 翻译进度和错误诊断。

无论采用何种实现，都必须保证 Markdown 结构安全、人工译文安全和源语言发布独立性。

---

## 10. AI Provider 与秘密配置

### 10.1 后台可视化配置

- API Provider、Base URL、模型和 API Key 必须在后台配置和选择。
- 不要求普通用户手工编辑配置文件。
- 后台必须提供连接测试和清晰的错误信息。
- 可视化界面不得回显完整 API Key。

### 10.2 API Key 存储

- API Key 允许以明文形式保存在服务器本地。
- 应保存在独立秘密文件中。
- 秘密文件和目录必须使用严格文件权限。
- API Key 不得进入日志、审计记录、错误响应、静态输出或公开 API。
- 普通备份和导出包不得包含 API Key。
- 恢复备份后由管理员重新填写秘密配置。

### 10.3 尚未最终确认的 Provider 产品细节

以下方案可作为实现建议，但尚未由项目所有者逐项确认：

- 是否同时保存多个 Provider；
- 是否设置默认 Provider 与故障回退顺序；
- 是否允许每次批量任务临时选择 Provider；
- 是否自动读取模型列表；
- 是否为不同语言设置不同模型。

---

## 11. 媒体与附件

- 媒体只保存在服务器本地文件系统。
- 第一阶段不支持 S3、R2、OSS 或其他对象存储。
- 媒体文件是永久数据，必须纳入普通站点备份。
- 删除缓存、数据库或生成目录不得影响媒体原件。
- 文章编辑器、主题设置和站点设置共用同一媒体库体验。
- 媒体管理界面与操作逻辑以 Halo 附件管理为产品基线。

---

## 12. 主题系统

### 12.1 主题管理体验

主题管理完整复刻 Halo 的产品逻辑，包括：

- 从本地压缩包安装；
- 从远程 URL 下载并安装；
- 已安装主题列表；
- 选择主题；
- 主题预览；
- 启用主题；
- 动态主题设置表单；
- 重载主题配置；
- 重置主题设置；
- 升级主题；
- 卸载主题；
- 卸载并删除主题配置；
- 为文章、页面和分类提供主题自定义模板。

Halo 主题管理参考：<https://docs.halo.run/2.19/user-guide/themes>

### 12.2 MutiBlog 自有主题格式

- 不直接运行 Halo Thymeleaf 主题。
- 主题使用适合静态发布的 MutiBlog React SSR 格式。
- 可以借鉴 Halo `theme.yaml` 的概念与管理体验。
- 后续开发的 MutiBlog 主题必须支持一键导入和替换。
- 如未来需要兼容 Halo 主题，应另行开发迁移工具，而不是在运行时兼容 Thymeleaf。

### 12.3 默认主题

- 首发默认主题复刻 Halo 默认主题 Earth 的布局、视觉和交互。
- 使用 MutiBlog 品牌，不使用 Halo 名称与 Logo。
- 默认主题必须原生支持全部已启用语言。
- 默认主题必须适配静态生成、语言跳转、搜索和动态评论降级。

---

## 13. 插件与扩展边界

- 第一版不开发通用插件运行时。
- 第一版不开发应用市场。
- 评论、搜索、AI 和多语言均为系统原生能力。
- 主题是第一版唯一允许安装的扩展类型。
- 未来如需要插件，必须重新设计适合文件真相源和静态发布的扩展协议。
- 不复制 Halo 的 Java 插件机制。

---

## 14. Git、备份与迁移

### 14.1 Git 友好但不内置 Git 客户端

- 内容目录可以由用户自行纳入 Git 管理。
- 用户可以在服务器上直接修改 Markdown/YAML。
- 系统应检测外部文件变化并重新索引。
- 后台可以提示内容来自外部修改。
- 第一版不内置 GitHub 登录、commit、push、pull 或 Git 冲突解决。

### 14.2 备份原则

普通备份至少包含：

- Markdown/YAML 内容；
- 评论 YAML；
- 媒体原件；
- 非秘密站点配置；
- 主题包与主题设置；
- 重建所需的版本信息。

普通备份明确不包含：

- AI API Key 和同类外部服务访问密钥；
- 可重建缓存；
- 可重建静态输出；
- 可重建 SQLite 索引。

管理员密码哈希、会话签名密钥等认证数据是否进入普通备份，尚未最终确认。

---

## 15. 开源与品牌

- 项目正式名称为 **MutiBlog**。
- `mutiblog` 少一个字母 `l` 是有意品牌设计，不是拼写错误。
- 技术标识、仓库名、镜像名和命令名优先使用小写 `mutiblog`。
- 产品界面使用 `MutiBlog`。
- 项目面向公众开源发布。
- 项目许可证采用 **AGPL-3.0**。
- 新项目必须补充完整 `LICENSE`、版权说明和第三方依赖声明。

---

## 16. 新旧代码边界

- 旧实现已归档到远程 `old` 分支。
- 新 `main` 是独立根历史。
- 禁止将旧分支视作迁移来源或兼容目标。
- 禁止从旧分支复制代码、测试、文档、数据模型或 API 设计。
- 新项目可以根据需要选择、混用或替换后端语言。
- 新实现以本说明书、Halo/HedgeDoc 的公开产品行为和后续明确决定为依据。

---

## 17. 明确排除项

当前产品范围明确排除：

- 多站点和多租户；
- 多用户协作；
- RBAC；
- 审核团队工作流；
- Halo 插件兼容；
- 应用市场；
- 商城；
- 商品、订单与支付；
- 远程对象存储；
- 内置 Git 客户端；
- 直接运行 Halo 主题；
- HedgeDoc 实时多人协作；
- 依赖 CMS 动态渲染公开正文。

---

## 18. 待统一讨论的产品问题

以下问题被明确留到后续统一讨论，当前不得假定最终答案：

### 18.1 发布与修订

- 草稿、已发布、定时发布、下线和回收站的最终状态模型；
- 是否保留本地修订历史；
- 修订查看、比较和恢复方式；
- 自动保存与离线恢复策略。

### 18.2 评论产品细节

- 匿名评论字段；
- 邮箱与头像规则；
- 评论审核默认值；
- 回复层级；
- 管理员回复与标识；
- Markdown 支持范围；
- 反垃圾策略；
- 邮件通知与自助删除；
- 评论是否构建进静态 HTML。

### 18.3 AI Provider 高级策略

- 多 Provider 配置档案；
- Provider 故障回退；
- 模型选择层级；
- 额度与 Token 计划的后台呈现；
- 框架字典的 AI 补全流程。

### 18.4 发布与运维

- Docker 以外是否提供裸机安装；
- 官方支持的 CPU 架构；
- 自动升级与版本回滚；
- 默认反向代理与 HTTPS 方案；
- 静态站点与动态评论 API 的生产拓扑。

### 18.5 导入与兼容

- 是否提供 Halo 内容迁移；
- 是否支持 Hugo、Hexo、WordPress 等导入；
- 是否提供 Halo Earth 设置迁移；
- 主题开发 SDK 与兼容性承诺。

### 18.6 认证数据备份

- 管理员密码哈希是否进入普通备份；
- 恢复后是保留管理员登录，还是重新执行管理员设置；
- 会话签名密钥是否恢复，还是在恢复后重新生成并使旧会话失效。

---

## 19. 实施验收底线

无论最终技术栈如何，以下结果必须成立：

1. 用户不阅读文档也能按 Halo 式流程完成初始化、写作、管理和发布。
2. 后台不存在写死某一种源语言或固定目标语言列表的代码路径。
3. 所有公开实体都能进入多语言模型。
4. 源语言内容发布不依赖 AI 可用性。
5. 人工译文不会被自动覆盖。
6. 缺失译文按照已确认的语言链跳转到真实语言 URL。
7. 已发布正文在 CMS 宕机时仍然可访问。
8. 评论服务故障不会影响正文阅读。
9. 删除 SQLite 和生成目录后可以从永久文件重建。
10. 普通备份不包含 API Key，但包含全部永久内容和媒体。
11. 当前语言、目标语言、菜单、分类、标签和主题文案均由数据驱动。
12. 文章编辑始终保存标准 Markdown。
13. 粘贴图片能自动上传并插入 Markdown。
14. 主题可安装、预览、启用、升级、重置和卸载。
15. 单容器部署在低资源 VPS 上可以稳定运行。
16. 新实现不引用 `old` 分支。

---

## 20. 防偏差执行规则

后续每项设计或开发工作开始前，必须回答：

1. Halo 2.25 在产品层如何处理这个功能？
2. MutiBlog 的多语言、文件真相源和静态发布是否要求改变 Halo 的行为？
3. 这个变化是项目所有者已确认的，还是开发者自行假定的？
4. 是否意外引入了多用户、插件、数据库真相源或动态正文渲染？
5. 是否引入了语言硬编码？
6. CMS 宕机后，已发布正文是否仍可访问？
7. 是否可能覆盖人工译文或丢失永久文件？

发现以下情况时必须暂停并询问项目所有者：

- 两个已确认原则互相冲突；
- 用户可见行为无法从本文档或 Halo 基线确定；
- 需要扩大产品范围；
- 需要改变永久数据格式；
- 需要改变 URL 稳定性或语言回退规则；
- 需要引入新的外部基础设施；
- 需要改变备份或秘密数据边界。
